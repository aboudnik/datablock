package org.boudnik.datablock.core;

import java.util.function.Function;

/**
 * @author Alexandre_Boudnik
 * @since 02/13/2018
 */
public class Main {
    public static void main(String[] args) {
//        Block block = ((Function<Parameters, Block>) parameters -> new Block(parameters.getDimensions(), "")).apply(new Parameters(new Block(null, null), new Block(null, null)));
//
//        Block created = Block.create(parameters -> new Block(parameters.getDimensions(), ""), block);
    }
}
